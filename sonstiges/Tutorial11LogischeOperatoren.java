/*package TutorialFolder;

public class Tutorial11LogischeOperatoren {

	public static void main(String[] args) {
		int iqDenis = 133;
        int iqEinstein = 182;
        int iq_ein_Stein = 0;
        // Vergleiche
        if(iqDenis==iq_ein_Stein)
            System.out.println("Denis ist so dumm wie ein Stein");
        
        		
        if(iqEinstein>=iqEinstein)											//größer gleich
            System.out.println("Einstein hatte einen höhren oder den gleichen IQ wie Denis!");
        
        
        if(iqDenis<=iq_ein_Stein)											//kleiner gleich
        	System.out.println("Man, ist Denis wirklich dümmer oder gleich dumm wie ein Stein?");
       
        
        if(iq_ein_Stein<iq_ein_Stein)										//kleiner als
            System.out.println("Dies wird nie ausgegeben, da ein Stein nicht dümmer ist als ein Stein");
        
        
        if(iqEinstein>iq_ein_Stein)											//größer als
            System.out.println("Einstein war intelligenter als ein Stein!");
 
        // Verknüpfungen
        
        
        if(iqDenis==133 && iqEinstein==182)									//zwei bedingungnen im IF befehl müssen auftauschen sonst gibt es kein System.out.println!
            System.out.println("Beide Vergleiche stimmen, sonst wird das nicht ausgegeben");
        
        
        if(iqDenis==iq_ein_Stein || iqEinstein==182)						// entweder , oder prinzip. wenn der erste nicht gilt, könnte der zweite gelten!
            System.out.println("Mindestens einer der beiden Vergleiche stimmt");
       
        
        if(iq_ein_Stein!=0)													// Ungleich 0. 
            System.out.println("Wird leider nie aufgerufen, Stein bleibt Stein bleibt IQ=0");
    }
 
}
*/

